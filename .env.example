# Copy this file to `.env.local` for local development.
# On Cloudflare Pages, add these under Settings → Environment variables.

# OPTIONAL — Contact form.
# Create a free form at https://formspree.io and paste its ID here (the part
# after /f/ in your endpoint). If left empty, the contact form falls back to
# opening the visitor's email client (mailto).
NEXT_PUBLIC_FORMSPREE_ID=
